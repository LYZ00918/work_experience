# 删除文件 指令rm
    rm -rf .git   rm -rf 文件名
    rm: 删除 (remove) 命令。
    -r: 递归 (recursive) 删除目录及其内容。
    -f: 强制 (force) 删除，无需确认。
    -i	interactive	交互模式	删除前逐个询问确认
    -v	verbose	详细模式	显示删除过程，让你知道删了什么
    .git: 目标文件夹的名称。